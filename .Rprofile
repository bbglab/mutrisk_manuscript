options(warn = -1)

# Suppress ALL library/require messages including .onAttach() banners
library <- function(...) suppressPackageStartupMessages(base::library(..., quietly = TRUE))
require <- function(...) suppressPackageStartupMessages(base::require(..., quietly = TRUE))
